module.exports = ({ origin }) => ({
  allOf: `${origin}/common/allOf`,
  anyOf: `${origin}/common/anyOf`,
  const: `${origin}/common/const`,
  definitions: `${origin}/common/definitions`,
  exclusiveMaximum: `${origin}/common/exclusiveMaximum`,
  exclusiveMinimum: `${origin}/common/exclusiveMinimum`,
  items: `${origin}/common/items`,
  maxItems: `${origin}/common/maxItems`,
  maxLength: `${origin}/common/maxLength`,
  maxProperties: `${origin}/common/maxProperties`,
  maximum: `${origin}/common/maximum`,
  minItems: `${origin}/common/minItems`,
  minLength: `${origin}/common/minLength`,
  minProperties: `${origin}/common/minProperties`,
  minimum: `${origin}/common/minimum`,
  multipleOf: `${origin}/common/multipleOf`,
  not: `${origin}/common/not`,
  oneOf: `${origin}/common/oneOf`,
  pattern: `${origin}/common/pattern`,
  patternProperties: `${origin}/common/patternProperties`,
  properties: `${origin}/common/properties`,
  required: `${origin}/common/required`,
  validate: `${origin}/common/validate`,
  validation: `${origin}/common/validation`,
  tupleItems: `${origin}/common/tupleItems`,
  type: `${origin}/common/type`,
  uniqueItems: `${origin}/common/uniqueItems`
});
